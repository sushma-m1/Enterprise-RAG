apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: local-registry
spec:
  type: NodePort
  selector:
    app: registry
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: {{ insecure_registry.split(':')[1] | int }}

