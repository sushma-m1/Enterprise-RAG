{{- if not .Values.hierarchicalDataprep.enabled -}}
# Copyright (C) 2024-2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.textCompression.nameOverride | default (include "helm-edp.fullname" .) }}
  labels:
    {{- include "helm-edp.textCompression.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 9397
      targetPort: 9397
      protocol: TCP
      name: http
  selector:
    {{- include "helm-edp.textCompression.selectorLabels" . | nindent 4 }}
{{- end }}
