{{- if .Values.hierarchicalDataprep.enabled -}}
# Copyright (C) 2024-2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.hierarchicalDataprep.nameOverride | default (include "helm-edp.fullname" .) }}
  labels:
    {{- include "helm-edp.selectorLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.hierarchicalDataprep.replicas }}
  selector:
    matchLabels:
      {{- include "helm-edp.hierarchicalDataprep.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "manifest.podLabels" (list .Values.hierarchicalDataprep.nameOverride .) | nindent 6 }}
      {{- include "manifest.tdx.annotations" (list .Values.hierarchicalDataprep.nameOverride .) | nindent 6 }}
    spec:
      {{- include "manifest.tdx.runtimeClassName" (list .Values.hierarchicalDataprep.nameOverride .) | nindent 6 }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "helm-edp.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers:
        - name: wait-for-vllm-server
          image: alpine/curl
          imagePullPolicy: {{ .Values.hierarchicalDataprep.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          env:
            - name: VLLM_SERVER_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: edp-hierarchical-dataprep-configmap
                  key: VLLM_SERVER_ENDPOINT
          command:
            - sh
            - -c
            - |
                if [ -z "$VLLM_SERVER_ENDPOINT" ]; then
                  echo "Environment variable VLLM_SERVER_ENDPOINT is not set. Skipping the init container.";
                else
                  until curl -s $VLLM_SERVER_ENDPOINT; do
                    echo "waiting for vllm model server $VLLM_SERVER_ENDPOINT to be ready...";
                    sleep 2;
                  done;
                fi;
      containers:
        - name: {{ .Values.hierarchicalDataprep.nameOverride }}
          securityContext:
            {{- toYaml .Values.securityContextWithTemp | nindent 12 }}
          {{ if .Values.alternateTagging }}
          image: {{ .Values.hierarchicalDataprep.repository }}:{{ .Values.hierarchicalDataprep.name }}_{{ .Values.hierarchicalDataprep.tag | default .Chart.AppVersion }}
          {{ else }}
          image: {{ .Values.hierarchicalDataprep.repository }}/{{ .Values.hierarchicalDataprep.name }}:{{ .Values.hierarchicalDataprep.tag | default .Chart.AppVersion }}
          {{ end }}
          imagePullPolicy: {{ .Values.hierarchicalDataprep.pullPolicy }}
          env:
          {{- if .Values.proxy.httpProxy }}
            - name: http_proxy
              value: {{ .Values.proxy.httpProxy }}
          {{- end }}
          {{- if .Values.proxy.httpsProxy }}
            - name: https_proxy
              value: {{ .Values.proxy.httpsProxy }}
          {{- end }}
          {{- if .Values.proxy.noProxy }}
            - name: no_proxy
              value: {{ include "helm-edp.noProxyWithContainers" . | quote }}
          {{- end }}
          {{- if .Values.hierarchicalDataprep.environment }}
            {{- toYaml .Values.hierarchicalDataprep.environment | nindent 12 }}
          {{- end }}
          envFrom:
            - secretRef:
                name: vector-database-config
            - configMapRef:
                name: edp-hierarchical-dataprep-configmap
          ports:
            - name: http
              containerPort: 9399
              protocol: TCP
          volumeMounts:
            - mountPath: /tmp
              name: tmp
          livenessProbe:
            failureThreshold: 24
            httpGet:
              path: v1/health_check
              port: http
            initialDelaySeconds: 5
            periodSeconds: 60
          readinessProbe:
            httpGet:
              path: v1/health_check
              port: http
            initialDelaySeconds: 5
            periodSeconds: 60
          startupProbe:
            failureThreshold: 120
            httpGet:
              path: v1/health_check
              port: http
            initialDelaySeconds: 5
            periodSeconds: 60
          resources:
            {{- $defaultValues := toYaml .Values.hierarchicalDataprep.resources | nindent 12 -}}
            {{- include "manifest.getResource" (list .Values.hierarchicalDataprep.nameOverride $defaultValues .Values) | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
{{- end }}
