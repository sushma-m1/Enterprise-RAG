{{- if not .Values.hierarchicalDataprep.enabled -}}
# Copyright (C) 2024-2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.textExtractor.nameOverride | default (include "helm-edp.fullname" .) }}
  labels:
    {{- include "helm-edp.textExtractor.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 9398
      targetPort: 9398
      protocol: TCP
      name: http
  selector:
    {{- include "helm-edp.textExtractor.selectorLabels" . | nindent 4 }}
{{- end }}
